<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">

    <insert id="saveFlavors">
        INSERT INTO dish_flavor(dish_id, name, value)
        VALUES
        <foreach collection="dishFlavors" item="dishFlavor" separator=",">
            (#{dishFlavor.dishId}, #{dishFlavor.name}, #{dishFlavor.value})
        </foreach>
    </insert>

    <select id="getByDishId" resultType="com.ellaskitchen.entity.DishFlavor">
        select * from dish_flavor
        <where>
            id = #{id}
        </where>
    </select>

    <delete id="deleteByDishId">
     delete from dish_flavor
     <where>
     dish_id in 
            <foreach collection="list" item="dishId" open="(" separator="," close=")">
                 #{dishId}
            </foreach>


     </where>

    </delete>





</mapper>